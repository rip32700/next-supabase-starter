export const publicRoutes = ['/', '/error']

// these routes will redirect logged in users to /settings
export const authRoutes = ['/auth/login', '/auth/register', '/auth/confirm', '/auth/callback']

export const DEFAULT_LOGIN_REDIRECT = '/account'
